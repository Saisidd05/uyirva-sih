export default function PriceSuggestionCard({ suggestion, onAccept, onOverride }) { if (!suggestion) return null; return <section className="price-suggestion"><p>AI fair-price suggestion</p><strong>₹{suggestion.recommended_min}–₹{suggestion.recommended_max}/{suggestion.unit}</strong><small>{Math.round(suggestion.confidence * 100)}% confidence · {suggestion.source}</small><div><button type="button" onClick={() => onAccept((suggestion.recommended_min + suggestion.recommended_max) / 2)}>Accept suggestion</button><button type="button" onClick={onOverride}>Set my price</button></div></section>; }
