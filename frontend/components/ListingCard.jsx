export default function ListingCard({ listing, onOrder }) { return <article className="listing-card"><img src={listing.photo_url || '/placeholder-produce.jpg'} alt={listing.crop} /><h2>{listing.crop}</h2><p>{listing.quantity} kg · ₹{listing.price}/kg</p><p>{listing.location || 'Farmgate location'} · Quality: {listing.quality_score || 'Not graded'}</p><button onClick={() => onOrder(listing)}>Place order</button></article>; }
