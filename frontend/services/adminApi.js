const request = async (path, options = {}) => { const response = await fetch(`${import.meta.env.VITE_API_URL || ''}${path}`, { ...options, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${localStorage.getItem('uyirva_access_token')}`, ...options.headers } }); const data = await response.json(); if (!response.ok) throw new Error(data.detail || 'Request failed'); return data; };
export const adminApi = { dashboard: () => request('/api/admin/dashboard'), verifications: () => request('/api/admin/verifications'), decideVerification: (id, decision) => request(`/api/admin/verifications/${id}`, { method: 'POST', body: JSON.stringify({ decision }) }), disputes: () => request('/api/admin/disputes'), resolveDispute: (id, body) => request(`/api/admin/disputes/${id}/resolve`, { method: 'POST', body: JSON.stringify(body) }), config: () => request('/api/admin/market-config'), saveConfig: body => request('/api/admin/market-config', { method: 'PUT', body: JSON.stringify(body) }) };
