const request = async (path, options = {}) => { const response = await fetch(`${import.meta.env.VITE_API_URL || ''}${path}`, { ...options, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${localStorage.getItem('uyirva_access_token')}`, ...options.headers } }); const data = await response.json(); if (!response.ok) throw new Error(data.detail || 'Request failed'); return data; };
export const buyerApi = { dashboard: () => request('/api/buyer/dashboard'), search: filters => request(`/api/buyer/listings?${new URLSearchParams(filters)}`), listing: id => request(`/api/buyer/listings/${id}`), favorite: id => request(`/api/buyer/listings/${id}/favorite`, { method: 'POST' }), matches: filters => request('/api/buyer/matches', { method: 'POST', body: JSON.stringify(filters) }), placeOrder: body => request('/api/orders', { method: 'POST', body: JSON.stringify(body) }), history: () => request('/api/orders'), tracking: id => request(`/api/orders/${id}/tracking`), confirmReceipt: id => request(`/api/orders/${id}/confirm-receipt`, { method: 'POST' }), dispute: (id, body) => request(`/api/orders/${id}/disputes`, { method: 'POST', body: JSON.stringify(body) }), rate: (id, body) => request(`/api/orders/${id}/rating`, { method: 'POST', body: JSON.stringify(body) }), profile: () => request('/api/buyer/profile'), saveProfile: body => request('/api/buyer/profile', { method: 'PUT', body: JSON.stringify(body) }) };
