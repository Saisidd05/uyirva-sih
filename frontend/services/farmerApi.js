const request = async (path, options = {}) => { const response = await fetch(`${import.meta.env.VITE_API_URL || ''}${path}`, { ...options, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${localStorage.getItem('uyirva_access_token')}`, ...options.headers } }); const data = await response.json(); if (!response.ok) throw new Error(data.detail || 'Request failed'); return data; };
export const farmerApi = { dashboard: () => request('/api/farmer/dashboard'), suggestPrice: listing => request('/api/farmer/price-suggestion', { method: 'POST', body: JSON.stringify(listing) }), qualityScore: listing => request('/api/farmer/quality-score', { method: 'POST', body: JSON.stringify(listing) }), createListing: listing => request('/api/farmer/listings', { method: 'POST', body: JSON.stringify(listing) }), listings: () => request('/api/farmer/listings'), editListing: (id, body) => request(`/api/farmer/listings/${id}`, { method: 'PUT', body: JSON.stringify(body) }), relist: id => request(`/api/farmer/listings/${id}/relist`, { method: 'POST' }), syncDrafts: drafts => request('/api/farmer/listings/sync-drafts', { method: 'POST', body: JSON.stringify({ drafts }) }), matches: () => request('/api/farmer/matches'), buyers: filters => request(`/api/farmer/buyers${filters ? `?${new URLSearchParams(filters)}` : ''}`), messages: id => request(`/api/farmer/matches/${id}/messages`), sendMessage: (id, body) => request(`/api/farmer/matches/${id}/messages`, { method: 'POST', body: JSON.stringify(body) }), matchDecision: (id, body) => request(`/api/farmer/matches/${id}/decision`, { method: 'POST', body: JSON.stringify(body) }), orders: () => request('/api/farmer/orders'), payouts: () => request('/api/farmer/payouts'), profile: () => request('/api/farmer/profile'), notifications: () => request('/api/farmer/notifications') };
